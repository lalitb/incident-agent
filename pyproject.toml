[project]
name = "incident-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["litellm>=1.75,<2", "pydantic>=2.11,<3", "python-dotenv>=1,<2"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["agent*"]
